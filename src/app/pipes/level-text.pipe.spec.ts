import { LevelTextPipe } from './level-text.pipe';

describe('LevelTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelTextPipe();
    expect(pipe).toBeTruthy();
  });
});
